import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PlaylistsModal = ({ visible, onClose, playlists }) => {
  return (
    <View>
      <Text>PlaylistsModal</Text>
    </View>
  );
};

export default PlaylistsModal;

const styles = StyleSheet.create({});
